codecov:
  require_ci_to_pass: true

coverage:
  precision: 2
  round: down
  range: "90...100"
  status:
    project:
      default:
        target: 95%
        paths:
          - src/lab_sim/
    patch:
      default:
        target: 95%

ignore:
  - tests/
